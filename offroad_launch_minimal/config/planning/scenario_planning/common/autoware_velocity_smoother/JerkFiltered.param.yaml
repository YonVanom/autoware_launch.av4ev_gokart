/**:
  ros__parameters:
    jerk_weight: 0.1         # weight for "smoothness" cost (lower = more aggressive)
    over_v_weight: 10000.0   # weight for "over speed limit" cost — keep high
    over_a_weight: 200.0     # weight for "over accel limit" — relaxed from 500 for racing
    over_j_weight: 100.0     # weight for "over jerk limit" — relaxed from 200 for racing
    jerk_filter_ds: 0.1      # resampling ds for jerk filter
